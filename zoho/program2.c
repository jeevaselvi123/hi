#include<stdio.h>
#include<math.h>
int main(){
    int arr[10];
    int i,j,n;
    float var;
    printf("Enter the size of the array:");
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    for(i=0;i<n;i++){
       var=sqrt(arr[i]);
       if   
    }
}